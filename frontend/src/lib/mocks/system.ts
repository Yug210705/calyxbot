import { SystemStatus } from "../types/system";

export const mockSystemStatus: SystemStatus = {
  mode: "demo",
  message: "Calyx is running in demo mode using mock data. Connect a live source to start building real company memory.",
  lastSyncLabel: "2 minutes ago",
};
